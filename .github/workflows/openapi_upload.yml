name: OpenAPI Uploader

on:
  push:
    branches:
      - main

jobs:
  upload_openapi:
    runs-on: ubuntu-latest

    steps:
      - name: 🔽 Checkout do Código
        uses: actions/checkout@v3

      - name: 📦 Installing JBang
        run: |
          curl -Ls https://sh.jbang.dev | bash
          echo "$HOME/.jbang/bin" >> $GITHUB_PATH

      - name: 🚀 Executing Scripting JBang
        env:
          API_ENDPOINT: "https://webhook.site/06447631-b5b2-43a9-9f7d-028f0d3a4618"  # Altere para seu endpoint real
        run: jbang send_openapi.java
